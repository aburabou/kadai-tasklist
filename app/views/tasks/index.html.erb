<% if logged_in? %>
<h1><%= current_user.name %>のタスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクを登録する', new_task_path %>

<% else %>

<% end %>